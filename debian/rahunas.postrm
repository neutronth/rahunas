#!/bin/sh

set -e

if [ "$1" != "remove" ] && [ "$1" != "purge" ]; then
  exit 0
fi

case "$1" in
  purge)
    test -d "/var/log/rahunas" && rm -rf /var/log/rahunas
    ;;
  remove)
    ;;
esac

#DEBHELPER#
exit 0
